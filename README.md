Consideraciones para la tarea 2: 

-Los estilos en CSS se mantuvieron en el mismo HTML al igual que en la tarea 1 y no se hicieron como un archivo separado 
-Al momento de revisar, por favor considerar que el validador de html no reconoce el codigo en jinja y lo ve como un error, pero fuera de eso, ya están corregidos todos los errores que son puramente de html
-No hay un boton para volver a la portada a partir de "Agregar actividad" ya que este no se pedía explicitamente 
